{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "sas-wiki/schema/feeds.schema.json",
  "title": "wiki_feeds",
  "description": "뉴스 카드 — 서빙 시점에 걸러진 항목만 담는다(자르는 것은 getFeed({limit}) 다. 파일을 자르면 필터가 그 안에서만 맞는다). wire 응답이므로 env 는 P2 소관이며 내부 feeds 아티팩트와 구분된다.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "generatedAt", "sourceCommit", "items"],
  "properties": {
    "schemaVersion": { "type": "integer", "minimum": 1 },
    "generatedAt": { "type": "string", "minLength": 1 },
    "sourceCommit": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "items": { "type": "array", "items": { "$ref": "#/definitions/feedItem" } },
    "nextCursor": {
      "anyOf": [{ "$ref": "#/definitions/feedCursor" }, { "type": "null" }]
    }
  },
  "definitions": {
    "feedItem": {
      "type": "object",
      "description": "정확히 7키.",
      "additionalProperties": false,
      "required": ["id", "ts", "title", "body", "keywords", "docs", "importance"],
      "properties": {
        "id": { "type": "string", "pattern": "^[0-9a-f]{12}$" },
        "ts": { "type": "string", "minLength": 1 },
        "title": { "type": "string", "minLength": 1 },
        "body": { "type": "string" },
        "keywords": { "type": "array", "items": { "type": "string" } },
        "docs": { "type": "array", "items": { "$ref": "#/definitions/docRef" } },
        "importance": { "type": "string", "enum": ["normal", "highlight", "breaking", "fix"] }
      }
    },
    "docRef": {
      "type": "object",
      "description": "이 커밋이 고친 문서 — path 가 아니라 **불변 id** 로 가리킨다(문서 이동 내성).",
      "additionalProperties": false,
      "required": ["id"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
        }
      }
    },
    "feedCursor": {
      "type": "object",
      "additionalProperties": false,
      "required": ["ts", "feedId"],
      "properties": {
        "ts": { "type": "string", "minLength": 1 },
        "feedId": { "type": "string", "pattern": "^[0-9a-f]{12}$" }
      }
    }
  }
}
